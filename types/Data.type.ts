import { FieldValues } from "react-hook-form";

export type DataProp = {
  manufacturing: string;
  maturity: string;
  perishable: boolean;
  product: string;
  quantityValue: string;
  price: string;
};
